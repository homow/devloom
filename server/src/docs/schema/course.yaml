CreateCourse:
  required: true
  content:
    multipart/form-data:
      schema:
        type: object
        properties:
          data:
            type: object
            description: JSON payload
            properties:
              title:
                type: string
              description:
                type: string
          image:
            type: string
            format: binary
            description: Image file